# =========================
# Production / Development
# =========================

# Required: MySQL connection string (works with TiDB Cloud / PlanetScale / etc.)
# Format example:
#   mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=

# Optional (recommended): JWT signing secret for auth cookies
JWT_SECRET=change-me-to-a-long-random-string

# Optional: run drizzle migrations automatically on app start
# Set to 0 to disable
MIGRATE_ON_START=1

# Port is provided by most hosts (Render/Fly/Railway). Locally you can set it.
PORT=3000
